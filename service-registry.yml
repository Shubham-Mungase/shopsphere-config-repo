server:
  port: 8761

spring:
  application:
    name: service-registry

  config:
    import: optional:configserver:http://config-server:8888  # ⚡ Use container name

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

  server:
    enable-self-preservation: true
